{
  "name": "openphotoid-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "OpenPhotoId for iPhone, iPad and Android: the web app's UI over the native Rust pipeline, packaged by Tauri 2.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "android:init": "tauri android init",
    "ios:init": "tauri ios init",
    "android:build": "tauri android build --apk",
    "ios:build": "tauri ios build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0"
  }
}
