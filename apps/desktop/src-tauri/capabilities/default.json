{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for OpenPhotoId's main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart",
    "dialog:default",
    "fs:allow-write-file",
    "fs:scope-home-recursive"
  ]
}
